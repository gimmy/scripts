#
# ~/.bash_aliases
#
# some more aliases

alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'

alias s='sudo '
alias ss='s -s'

alias o='xdg-open'
alias diff='diff -y --suppress-common-lines'
alias archlinux='lynx www.archlinux.org'

